[project]
name = "grantguard"
version = "0.1.0"
description = "Audit and clean Claude Code permission allowlists."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = []

[project.scripts]
grantguard = "grantguard._entry:main"

